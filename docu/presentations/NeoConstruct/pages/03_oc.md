---
title: Optimal Control
layout: image-right
class: text-right
image: /recordings/arm_plot.png
---
<div class="h-60"/>

# Optimal Control

#### with Acados

<!-- highlighter -->
<div v-click="1" class="text-left border-2 border-solid border-orange500 absolute right-26em top-3.4em text-orange500" style="width:3em; height:8.4em;"><code>q</code> &nbsp</div>
<div v-click="1" class="text-left border-2 border-solid border-orange500 absolute right-26em top-11.7em text-orange500" style="width:3em; height:8.4em;"><code>q̇</code> &nbsp</div>
<div v-click="1" class="text-left border-2 border-solid border-lime500 absolute right-26em top-20.2em text-lime500" style="width:3em; height:8.4em;"><code>τ</code> &nbsp</div>

---
title: Acados Code 1
layout: two-cols-header
class: text-right
---
<div class="h-10"/>
::left::
<div class="h-10"/>
<h1 class="text-purple-500">Acados Setup</h1>
<div class="h-5"/>

$$
\begin{align}
x(t) &=
\begin{bmatrix}
q(t) \\ \dot q(t) \\
\end{bmatrix}
\\
\nonumber \\
u(t) &= \tau(t) \\
\nonumber \\
f_{expl}(x(t), u(t), t) & =
\begin{bmatrix}
\dot q(t) \\ \ddot q(t) \\
\end{bmatrix} \\
&=\, \dot x(t) \nonumber
\end{align}
$$

::right::
<div class="h-20"/>
<div class="text-left">

````md magic-move {at: 1,lines: true}
```python
# Independent Terms
q, qdot, tau
# Dependent Terms
qddot, p
```

```python {6-7}
# Independent Terms
q, qdot, tau
# Dependent Terms
qddot, p

# Core Acados Object
ocp = AcadosOcp()
```
```python {9-11|12-15}
# Independent Terms
q, qdot, tau
# Dependent Terms
qddot, p

# Core Acados Object
ocp = AcadosOcp()

# Acados Model
ac_model = AcadosModel()
ac_model.name = "simple_arm"
#     Problem Formulation
ac_model.x = ca.vertcat(q, qdot)
ac_model.u = tau
ac_model.f_expl_expr = ca.vertcat(qdot, qddot)
```
````

</div>

<div class="h-2"/>
<div v-click="4" class="text-left">
  <i class="text-purple500">implicit: &nbsp</i>
  <code >f_impl_expr</code><br>
  <i class="text-purple500">explicit: &nbsp&nbsp</i>
  <code >f_expl_expr</code><br>
  <i class="text-purple500">discrete:&nbsp</i>
  <code >disc_dyn_expr</code>
</div>
<!-- layout modifier -->

<style>
.two-cols-header {
  column-gap: 100px;
}
</style>

---
title: Acados Code 2
layout: two-cols-header
class: text-right
---
<div class="h-10"/>
::left::
<div class="h-10"/>
<h1 class="text-purple-500">Acados Cost Function</h1>
<div class="h-5"/>

$$
\begin{align}
\min_{x(.),\ u(.)} & \int_0^{T_f} \| p(t) - p_{\text{target}} \|^2_{I_3} \ dt \qquad \\
\end{align}
$$

<v-switch>
  <template #1>
    <div class="h-10"/>
    <div class="text-center" style="font-size: 30px">
    <code class="text-purple500">cost</code>
    <code class="text-sky500">y_expr</code>
    <code class="text-lime500">e</code>
    </div>
    <div class="h-5"/>
    <div class="text-center" style="font-size: 30px">
    <i class="text-purple500">class </i>
    <i class="text-sky500">attribute </i>
    <i class="text-lime500">time </i>
    </div>
  </template>
  <template #2>
    <div class="h-10"/>
    <div class="text-center">
      <code class="text-purple500">AUTO</code><br>
      <code class="text-sky500">EXTERNAL</code><br>
      <code class="text-lime500">LINEAR_LS</code><br>
      <code class="text-orange500">NONLINEAR_LS</code><br>
      <code class="text-pink500">CONVEX_OVER_NONLINEAR</code>
    </div>
  </template>
  <template #3>
    <div class="h-10"/>
    <b>General Formulation:</b>

$$
\begin{align}
\| y(t) -y_{ref} \|^2_{W}
\end{align}
$$

  </template>
</v-switch>

::right::
<div class="h-40"/>
<div class="text-left">

````md magic-move {at: 1,lines: true}
```python {1-2|4-5|4-5}
# Acados Cost Object
ocp.cost = AcadosOcpCost()

# tf: ||p - p_target||² (p: endeffector position)
ocp.cost.cost_type_e = "NONLINEAR_LS"

```

```python {7-9}
# Acados Cost Object
ocp.cost = AcadosOcpCost()

# tf: ||p - p_target||² (p: endeffector position)
ocp.cost.cost_type_e = "NONLINEAR_LS"

ac_model.cost_y_expr_e = p
ocp.cost.yref_e = p_target
ocp.cost.W_e = np.eye(3)
```
````

</div>

<!-- layout modifier -->

<style>
.two-cols-header {
  column-gap: 100px;
}
</style>

---
title: Acados Code 3
layout: two-cols-header
class: text-right
---
<div class="h-10"/>
::left::
<div class="h-10"/>
<h1 class="text-purple-500">Acados Constraints </h1>
<div class="h-5"/>

$$ {*|1|2-3|4|*}
\begin{align}
&\quad \ x(0) = 0_{N_q} \\
\left[-v_{\text{max}}\right]_{N_q} &
\leq
\dot q(t) \leq
\left[v_{\text{max}}\right]_{N_q} \\
\left[-\tau_{\text{max}}\right]_{N_q} & \leq u(t) \leq \left[\tau_{\text{max}}\right]_{N_q} \\
\left[-v_\text{static}\right]_{N_q} & \leq \dot q(T_f) \leq \left[v_\text{static}\right]_{N_q} \\
\end{align}
$$

<div class="h-10"/>
<v-switch at="0">
  <template #2>
    <b>General Formulation:</b>

$$
\begin{align}
\qquad \underline x \leq J_{bx} \ x(t) \leq \overline x \\
\end{align}
$$

  </template>
  <template #4>

> **Nonlinear Constraints:**
>
> $h(x,\ u,\ p,\ p_{\text{global}} )$

> **Linear Constraints:**
>
> $g(x,\ u,\ p,\ p_{\text{global}} )$

  </template>
</v-switch>

::right::
<div class="h-0"/>
<div class="text-left">

````md magic-move {at: 1,lines: true}
```python
# Acados Constraint Object
ocp.cons = AcadosOcpConstraints()
```
```python {4-5}
# Acados Constraint Object
ocp.cons = AcadosOcpConstraints()

# initial
ocp.cons.x0 = np.zeros(nq * 2)
```
```python {1-2,10-18}
v_max   = 1.0 # m/s
tau_max = 1.0 # Nm

# Acados Constraint Object
ocp.cons = AcadosOcpConstraints()

# initial
ocp.cons.x0 = np.zeros(nq * 2)

# path
#       limit controls
ocp.cons.idxbu = np.ones(nq)
ocp.cons.ubu   = np.full(nq, tau_max)
ocp.cons.lbu   = np.full(nq, -tau_max)
#       limit velocities
ocp.cons.idxbx = np.arange(nq,2*nq)
ocp.cons.ubx   = np.full(nq, v_max)
ocp.cons.lbx   = np.full(nq, -v_max)
```
```python {21-24}
v_max    = 1.0 # m/s
tau_max  = 1.0 # Nm
v_static = 1.0 # m/s

# Acados Constraint Object
ocp.cons = AcadosOcpConstraints()

# initial
ocp.cons.x0 = np.zeros(nq * 2)

# path
#       limit controls
ocp.cons.idxbu = np.ones(nq)
ocp.cons.ubu   = np.full(nq, tau_max)
ocp.cons.lbu   = np.full(nq, -tau_max)
#       limit velocities
ocp.cons.idxbx = np.arange(nq,2*nq)
ocp.cons.ubx   = np.full(nq, v_max)
ocp.cons.lbx   = np.full(nq, -v_max)

# terminal
ocp.cons.idxbx_e = np.arange(nq,2*nq)
ocp.cons.ubx_e   = np.full(nq, v_static)
ocp.cons.lbx_e   = np.full(nq, -v_static)
```
````

</div>

<!-- layout modifier -->

<style>
.two-cols-header {
  column-gap: 100px;
}
</style>

---
title: Acados Code 4
layout: two-cols-header
class: text-right
---
<div class="h-10"/>
::left::
<h1 class="text-purple-500">Acados Solver<br>Settings</h1>

<v-switch>
<template #0>
  <div class="flex justify-end">
    <img src="/recordings/arm_plot.png" width="300px"/>
  </div>
</template>
<template #4>
<div class="h-15"/>
<div>

```bash
# it    res_stat      res_eq    res_ineq    res_comp
 260  5.0831e-05  2.9258e-05  0.0000e+00  2.9653e-06
 261  2.8726e-05  5.6914e-05  0.0000e+00  2.7143e-06
 262  9.6503e-06  2.0013e-06  0.0000e+00  2.5040e-06
```

```bash
 ...  qp_stat  qp_iter  step_norm   lm_reg.     alpha
 ...        0        8   6.09e-02  0.00e+00  1.00e+00
 ...        0        8   4.41e-02  0.00e+00  1.00e+00
 ...        0        8   2.34e-02  0.00e+00  1.00e+00

Optimal solution found! Converged to KKT point.
```

</div>
</template>
</v-switch>

::right::
<div class="h-0"/>
<div class="text-left">

````md magic-move {at: 1,lines: true}
```python {1-5|7-8|10}
# Setup Solver
ocp.model = ac_model
ocp.solver_options.tf = Tf
ocp.solver_options.N_horizon = N
ocp.solver_options.integrator_type = "ERK"

ocp.solver_options.tol = 10**-5
ocp.solver_options.nlp_solver_max_iter = 1000

ocp.solver_options.print_level = 3 # 4 for qp iters
```
```python {15-17|19-23}
Tf = 1.0
N = 30

# Setup Solver
ocp.model = ac_model
ocp.solver_options.tf = Tf
ocp.solver_options.N_horizon = N
ocp.solver_options.integrator_type = "ERK"

ocp.solver_options.tol = 10**-5
ocp.solver_options.nlp_solver_max_iter = 1000

ocp.solver_options.print_level = 3 # 4 for qp iters

# Start solving
solver = AcadosOcpSolver(ocp)
solver.solve()

# Check Results
solver.print_statistics()
print("Cost:", solver.get_cost())
if solver.get_status() != 0:
    sys.exit(-1)
```

````

</div>

<!-- layout modifier -->

<style>
.two-cols-header {
  column-gap: 50px;
}
</style>

---
title: Final Result
layout: image-right
image: /recordings/arm-vertical.GIF
class: text-center
---
<h1>🎉 Final Result 🥳</h1>
<div class="flex justify-end">
  <img src="/recordings/arm_plot.png" width="500px"/>
</div>


---
title: Pipeline (Acados)
class: text-center
hide: True
---

<div class="h-20"/>

# Pipeline

<div class="h-10"/>

```mermaid
flowchart LR
  classDef pin_highlight fill:white,stroke:#0093ff,stroke-width:2px
  classDef ca_highlight fill:white,stroke:#ff6a00,stroke-width:2px
  classDef ac_highlight fill:white,stroke:#9C27B0,stroke-width:2px
  classDef none stroke:white,fill:white

    p["<wbr><img src="/TechStack/pinocchio_logo.png"/>Rigid Body Dynamics"]
    c["<wbr><img src="/TechStack/casadi.png"/>Computer Algebra"]
    a["<wbr><img src="/TechStack/acados_logo.png"/>Optimal Control"]

     p:::pin_highlight --- c:::ca_highlight -->a:::ac_highlight
```

---
title: Acados Meme
layout: image-right
image: acados_size.jpg
backgroundSize: contain
---

<div class="h-60"/>

# There is way more!
